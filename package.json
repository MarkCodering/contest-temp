{
  "private": false,
  "dependencies": {
    "@vercel/edge": "^0.1.2"
  },
  "scripts": {
    "postinstall": "git lfs install && git lfs pull",
    "build": "echo \"Static deploy: no build step needed ✔️\""
  }
}
